---
name: 'guild-states-audit'
description: 'Mage — Audit state coverage — does every element have all required states?'
---

IT IS CRITICAL THAT YOU FOLLOW THIS COMMAND: LOAD the FULL {project-root}/_bmad/guild/agents/visual-designer.md, READ its entire contents, activate as the Mage Visual Designer agent, then immediately execute menu item "SC" — Audit state coverage on the screen the user specifies. For every interactive element and data container, check: default/populated, empty (with guidance), loading (skeleton or spinner), error (with recovery action), disabled (with explanation), selected/active, and hover/press feedback states.
